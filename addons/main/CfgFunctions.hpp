class CfgFunctions {
    class MOD_CLASS {
        tag = QUOTE(MOD_TAG);

        class COMPONENT_CLASS {
            CFG_FUNCTION_PRE_INIT(setupFunctions);
            CFG_FUNCTION(loadFunctions);
        };
    };
};
